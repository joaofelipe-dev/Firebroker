import { Provider, Frame, Field, Select, Upload, Location, Severity, Toggle, Submit, Success, StateGate } from './ReportForm';

export const ReportForm = { Provider, Frame, Field, Select, Upload, Location, Severity, Toggle, Submit, Success, StateGate };
